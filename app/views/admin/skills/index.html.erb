All Skills:

<% @admin_skills.sort_by {|hash| hash[:name]}.each do |skill| %>
  <ul>
    <li><%= "#{skill["name"]}, #{skill["default_skill"]}, #{skill["skill_group"]} - used by #{skill["count"]} characters" %><li>
  <ul>
<% end %>
